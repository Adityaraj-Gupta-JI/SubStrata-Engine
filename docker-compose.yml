services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: substrata_backend
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      # Pass an external database string (Neon/Supabase) if needed in the future
      - DATABASE_URL=${DATABASE_URL:-}
    restart: unless-stopped